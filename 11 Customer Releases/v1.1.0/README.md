# TenantIQ Customer Release v1.1.0

This folder tracks the refreshed TenantIQ customer download package built from the latest validated assessment engine.

## Release metadata

- Version: v1.1.0
- Status: RELEASE READY
- Source branch: `agent/v1.1.0-release-hardening`
- Purpose: replace the previous customer download with the newest validated TenantIQ package without overwriting v1.0.0

## Release contents

The customer ZIP itself is generated by `Build-TenantIQPackage.ps1`. GitHub preserves the exact source snapshot by commit; this release folder stores the release record and verified build metadata rather than duplicating the full source tree.

## Release checklist

- PowerShell integrity remains protected
- Customer package version set to 1.1.0
- Customer-facing changelog updated
- Release package validation required
- Release candidate smoke test passed (14/14)
- Support bundle redaction self-test passed (6/6)
- ZIP SHA256 recorded after successful build
- `../latest/README.md` updated only after successful validation

## Notes

TenantIQ v1.1.0 passed the package builder's `RELEASE READY` and `RELEASE CANDIDATE READY` gates and is the active customer release.
